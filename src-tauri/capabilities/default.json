{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Control panel and tray: bundled pages only, full command IPC",
  "windows": ["main", "tray-panel"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "allow-app-ipc"
  ]
}
